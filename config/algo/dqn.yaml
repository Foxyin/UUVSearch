# UUVSearch - DQN 超参数配置
hidden_dim: 256
gamma: 0.99
lr: 1e-4
epsilon_start: 1.0
epsilon_min: 0.1                   # 保留 10% 探索
epsilon_decay_steps: 300000         # 线性衰减至 epsilon_min
buffer_size: 100000
batch_size: 64
learn_start: 1000
update_target_every: 1000